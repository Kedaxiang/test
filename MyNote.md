## 3月22日
### 学习内容
1. css选择符，上下文选择符用空格隔开，分组选择符用逗号隔开。
2. 子选择符>;紧邻同胞选择符+；一般同胞选择符~；通用选择符*，**另外一个用法**  `section * a{font-siaze:1.3em;} `
表示section孙子元素,非子元素的a标签被选中，与a的父元素无关。
3. 多类选择符 `<p class = "1 2"></p>`,放入一对引号，空格隔开。
4. id属性 `<a href="#bio">Bio</a>`,#id语法，可用于页内导航链接，目标下方应有对应元素id，`<h3 id="bio"></h3>`  
5. 属性选择符与属性值选择符，`标签名[属性名]`，`标签名[属性名="属性值"]`。
6. 伪类:        
    * :focus : 单击时获得焦点，用户输入字体 
    * :target : 单击指向页面中其他元素的链接，`<a href = "#bio"></a>`就是target
    * :first-child : 一组同胞元素第一个元素 
    * :last-child : 最后一个元素
    * :nth-child() : ()中填数字，表示第几项元素 
    * ::first-letter : 首字符 
    * ::first-line : 第一行 
    * ::before和::after : 特定元素的前后 
### 进度
1. git仓库终于千辛万苦弄好;
2. 完成切图;
3.  `em`和`%`相对于其父元素。
-----------------------------------------------
## 3月23日
### 学习进度
1. css中权重 `![属性值]空格`
2. flex布局大致了解
### 考核进度
完成第一二行
### 存在的问题
1. 使用了flex布局缩小窗口无法自适应，***可能原因是设计了固定像素***
2. 找不到对应字体
3. 使用css属性中background-image**无法加载背景图片**，不清楚原因。（**已解决**）
----------------------------------------------------------------
## 3月24日
### 学习进度
1. 熟悉flex布局并加以运用
2. `vw`：视窗宽度的百分比（1vw 代表视窗的宽度为 1%）；
   `vh`：视窗高度的百分比；
   `vmin`：当前 `vw` 和 `vh` 中较小的一个值；
   `vmax`：当前 `vw` 和 `vh` 中较大的一个值

   （1）`%` 是相对于父元素的大小设定的比率，`vw`、`vh` 是视窗大小决定的。
   （2）`vw`、`vh` 优势在于能够直接获取高度，而用 `%` 在没有设置 `body` 高度的情况下，是无法正确获得可视区域的高度的，所以这是挺不错的优势。
3. `min-width`属性：用于设置元素最小宽度
4. `line-height`不允许使用负值，设置行间的距离（可应用与块元素）
5. `<form>`标签创建表单，能够包含`input`元素
6. `top`规定元素顶部边缘，定义了一个定位元素的上外边距边界与其包含块上边界之间的偏移

### 考核进度
1. 完成加入图片，问题在于没有理清各个div的包含关系，以至于路径正确但无法现实背景图片。
2. 尝试运用了flex布局，完成input。
3. 完成侧栏，但还未实现交互。
### 存在的问题
1. 主要是运用了**太多**的div class 可能会打乱自己的思路。
2. 今天才意识到好多注释没写，明天再补了。
3. flex布局中，3个元素竖直居中排列，但是要是元素长度不一不会向左对齐，不知道该如何实现，所以我用了一个不太好的方案，但也算能解决问题。
4. 设计稿中是**1366x1195**，但我的屏幕是**1920x1080**，有些地方不清楚该如何调整。
5. 有些地方无法**自适应**，但我也不清楚解决方案。

-------------------------------------------------------------
## 周记
* 生活随记

  * 切图是临时学的，感觉也会有点不太精确，然后弄git确实浪费了特别多的时间，主要是当时还是不特别懂，那些操作，
  浪费了挺多时间的吧，然后自己的课也比较多，不仅要上课还有自己部门的活动要去参加，所以时间还是要靠自己挤出来的，但其实就这样学的东西确实很快，有项目去压迫着完成，遇到问题尝试多种解决方案，看看那种最适合，但其实说实话，**我感觉我还真的很难做到严格按照设计图**，但位置会尽量做到位吧，很尴尬，也是自己的水平还没有到达那个层次，不过要是真的很难的话，我会选择先把整个做好，要是还要微调之后继续吧，发现其实做网页也真的很需要耐心，很多夹杂在一起真的你会不知道有什么情况发生，**必须自己亲自打demo**，才能知道那种是比较适合的吧。总之现在还有5天，希望最后能把任务完成吧。

* 学习进度
  
  * 主要就是每天记录的东西了。

* 学习难点

  * 同上

* 不足之处

  * 浪费了大量时间，主要被社团活动所浪费，参加活动却没有把时间落到实处。
  * 也确实会不专心，打着打着打烦了，就去看看番啥的了。
  * 安排时间也不太恰当，会有点与自己的作业有点冲突...

* 下星期的学习安排

 * 希望能更了解css以及flex布局，感觉并没有学精，只是简单运用。
 * 分好时间段，什么时间用来写自己的课外作业，把其他时间挤出来赶任务。
 * 希望脑子能好用点，别在一个傻逼的地方卡太久
 * 把注释补上，方便查阅代码。
 ------------
 ## 3月25日
 ### 学习进度
 1. 怎么说，应该是对flex布局有了更深一点的理解吧，主要是各种的嵌套关系一定要理清楚。
 2. 每个CSS第一个样式`*(margin: 0; padding: 0;)`，`*`表示对所有元素，先把**所有**元素的外边距和内边距都去掉，再在**需要边距**的元素增加边距。
 3. `em`可以使段间距对字号的变化而变化。
 4. 不设置`width`，`auto`**与父元素同宽**。
 5. `overflow:hidden`的几个作用：
     * 防止包含元素被超大内容撑大，应用之后，元素依然保持设定宽度，而超大的子内容，会被容器切掉。
     * 能迫使父元素包含其浮动元素。

### 考核进度
1. 现在就只差`footer`了，目前认为同样需要使用flex布局实现。
2. 完成页尾之后还需要微调一些小的边距，以便达到设计图的效果。
3. 还未加入交互作用，目前希望明天将整个布局坐完，再把一些**需要用CSS实现交互**的地方做完，然后开始继续学JS，总体来说今天大概达到预期。

### 存在的问题
1. flex布局真的需要非常多的**嵌套**，尤其要注意理清**各个嵌套**之间什么包含什么，理好思路，写下注释，方便思考，才能用好。
2. 有些地方对于**盒子模型**还不太熟练，需加点小边距，有些地方我觉得是可以又方便快捷的办法的，但可以还是不会熟悉的运用，所以就仔细按照设计图数像素，利用`margin`和`padding`来解决问题了，但可能并不会是最优解。
--------
## 3月26日
### 学习进度
1. 使用`hover伪类时`，例如`li:hover a{color: white;}`，此时的`li`为`a`的父元素，才会在光标移动至目标元素时改变其颜色。
2. `position:relative`与`position:absolute`，后者的父元素若被设定为`position:relative`，则当其设定了`positon:absolute`时，后者的位置将会**相对于前者**改变。若未设定，则会**相对于后者的祖先元素**。
### 考核进度
1. 静态布局已经全部完成。
2. CSS功能全部加入。
3. 剩下的JS希望能在明天解决，然后自己重温一遍代码，备好注释，挑出一些问题，询问师兄师姐。
### 存在的问题
1. 像素还是很难**拿捏**
2. 部分元素当窗口**变窄**时，还是无法完成自适应，比如在我的作品examine.html中，nav标签的元素在窗口变窄时会挤在一堆。（姑且想到一个解决方案是利用min-width，设定最小宽度）
-------------
## 3月27日
### 学习进度
1. 利用JS，更改选项卡，并且增加/删除类名，利用`classList.remove`和`classList.add`来控制删除和增加；更改选项卡其实就是**改变其style**，利用数组，要注意，JS的数组都是从0开始，例如i[4]包含0,1,2,3。
2. `window.onload`，表示事件会在页面加载完成后触发。
3. JS中`this`的用法
   * 在一般函数方法中使用 this 指代全局对象
   * 作为对象方法调用，this 指代上级对象
4. CSS中cursor属性控制光标的类型。
### 考核进度
     全部完成，功能完善，但部分无法实现自适应。
### 存在的问题
1. 有些元素还是无法实现自适应，比如`nav`，`background-img`,中部的更多资讯，根据我所写到的样式，`nav`使用flex布局，而剩下两个都是利用`center`来实现的，所以后面两个会严格按照所写样式执行，不太了解解决方案。
2. 有些地方按照像素，但看起来还是和设计图看起来不相符，但也是按照设计图，数了像素，所以利用了很多盒子模型，不知道这是不是最佳的解决方案。
3. 字体利用`lighter`和`bolder`也无法实现设计稿中的样子，不太了解。
--------------------------------------
## 到今天为止的总结
    怎么说，5天时间完成，但其实自己也明白，虽然当页面完全放大时，还是差不多的，但是一缩小页面，有些地方无法实现自适应，但也不太清楚比较好的解决方案，这是我所欠缺的，所以还是需要多学习吧，了解解决方案，完善好自适应，感觉才差不多吧。当然，完成了自适应之后，还想加一些css的动画，然后想了解一下在移动设备的布局。毕竟也挺想在这条路上走的更远...
------------------
## 3月28日
### 学习进度
* 今天的话就没有过多学习多的东西，花了一天把前几天自己上课欠的内容和作业补完。
* 不过说实在的，还是有很多地方不是很懂，长路漫漫，还需要我慢慢学习。
### 考核进度
* 已经将所有布局，功能完成，算是做得差不多了，但是还是存在很多不足，需要我慢慢调试。
* 解决了几个问题，上端的`nav`和中间的更多资讯，`nav`设定完固定的宽度之后，缩小窗口便不会挤到下面，而`更多资讯`，可以利用flex布局，`wrap`换行实现，再利用`justify-content: center;`，可以实现居中对齐来解决问题。
### 存在的问题
1. 第一个便是我布局的问题，当我缩小宽度时，我的内容确实会居中，但是当我拉动横向的滚动条时，我的左边却空出一大片空白，这是我特别特别不足的地方，应该是布局问题，一部分的确会自适应，但自适应只会和当前的页面大小适应，而不会和滚动条旁边的空白位置适应，所以由于我的能力实在有限加上时间不充裕，可能难以做到完全的自适应布局，所以目前的打算是完完全全定死的布局，来完善一下我这个四不像。
2. 但是当我完成一定的固定的布局之后，缩小窗口，我的左外边距会比右外边距大，并不明白问题出在哪里。
3. 目前我做的大部分都已经被定死，不会随着窗口的缩小而改变，而我上部分的导航依旧会**假装**的自适应，所以目前的想法是加入几个`span`标签，加入`margin`设置左右外边距来完全定死。
4. 希望剩下的几天在能够加入一些交互和一些动画效果...
-------------------
## 3月29日
### 学习进度
1. 为了想在登录和注册加点功能，差了点资料，利用`alert()`事件和`prompt()`制造弹窗，`alert`为弹出一个提醒框，`prompt`制造了一个具有输入功能的弹窗。
2. CSS3动画，`transform`属性向元素应用 2D 或 3D 转换。该属性允许对元素进行旋转、缩放、移动或倾斜,`scaleX()`通过设置 X 轴的值来定义缩放转换;`scaleY(y)`通过设置 Y 轴的值来定义缩放转换。
3. transition 属性是一个简写属性，用于设置四个过渡属性：
    * transition-property,设置过渡效果的 CSS 属性的名称(none;all;css属性名).
    * transition-duration,完成过渡效果需要多少秒或毫秒.
    * transition-timing-function,允许过渡效果随着时间来改变其速度.
    * transition-delay,定义过渡效果何时开始(开始之前需要等待的时间).
4. `::after`伪类，一般会有`content`属性，来插入生成内容。
5. `position: fixed`生成绝对定位的元素，相对于浏览器窗口进行定位（此条为前几天所学，这里进行补充）。
### 考核进度
* 固定了大部分元素，加入了css3动画以及js不是很骚的骚操作。
### 存在的问题
* 没有完成自适应布局有点可惜，还有少许地方未修改，明天最后上交前会修改完整，固定好布局。
### 接下来计划
* 学习JS以及css的自适应布局吧，希望每天都有一点进步，争取进入工作室。
----------------
## 3月30日
### 学习进度
1. 字体变化利用`font-variant`，该属性除了normal以外，即small-caps，使所有的小写英文字母变成小型大写字母。
2. `font`简写规则：
    * `font-weight`,`font-style`,`font-vaiant`不分先后。
    * 后接`font-size`.
    * 后接`font-family`.
3. `line-height`行高，若字体`font-size`为16px，`line-height`设为20px，则会在每行上下空出4px，凑成20px。
4. `text-indent` 文本缩进，可继承，只对第一行有效。 
5.  * `transform`：css3中变形处理，实现文字，图像的旋转，缩放，移动等。
    * `transition`: 通过一份属性值平滑的过渡到另一个属性值，实现动画。
    * `animation`: 通过在样式中创建对个关键帧，实现复杂的动画。
------------------------
## 4月7日
### 学习进度
1.  主要还是复习了一下基础吧。`cursor: pointer;`可以改变光标的样式，光标呈现为指示链接的指针 **(一只手)**，这个其实挺实用的，在用`li`制造表单配合js时，可以切换内容，将这个样式加载`li`里，可以方便用户交互。
2. `animation`的更深理解：
    * 语法：`animation: name duration timing-function delay iteration-count;`
    * 说明:
        1. `animation-name`：            动画名称;
        2. `animation-duration`：        动画完成时间;
        3. `animation-timing-function`： 动画完成周期的样式;
        4. `animation-delay`：           动画延迟多久开始执行;
        5. `animation-iteration-count`:  动画的播放次数.
    * 关于其他的属性我**暂时**还没有用到实处，所以这里就没有记录了。
3. 登录框一般都是用HTML`form`标签完成，利用js控制显隐。
### 考核进度
1. 小作业在6号完成，做的是圆环加载的进度条。主要利用了两个长方形，这两个长方形可以凑成一个正方形。**长方形**里**套着**正方形的`div`,也就是长方形为**父元素**，里面的正方形左为左上和左下的`border`，并且`radius`设置为**50%**,加上`overflow:hidden`，在旋转的过程中，当左边的半圆转到右边时，被隐藏，右边同理。由此得到的动画效果。
2. 考核进度第一个主页算是完成的差不多了，还有一些小功能比如动画还没加进去，但是表单的登录已创建。
### 存在的问题
1. Loading动画是真正意义上的动画还是专门创建一个动画，动画结束后隐藏。（4月8号解决）
------------------------
## 4月8日
### 学习进度
1. 图片的自适应利用以下样式可以达到   
            `width:auto;`
            `height:auto;`
            `max-width:100%;`
            `max-height:100%;`
    * 可以保证图片的大小随窗口变化而变化，但横向放大时貌似还是有点缺陷。
2. js以及ajax，了解大部分，但是基本上我所了解的教程都需要一个后台服务器，我不知道我们考核需不需要自己写出一个php这类的东西来。
### 考核进度
1. 由于今天满课，实在有点难受，所以其实进度上并没有取得太大进展，再加上Ajax需要学习，所以我今天完成的部分就是有Loading动画了，利用`animationend`事件来是的cover层消失，添加其样式为`display:none;`，即可完成。其中居中利用了flex布局。
### 存在的问题
1. 我们真的要自己编写一个php嘛？！
----------------------
## 4月9日
### 学习进度
1. 基本将Ajax的原理以及操作了解差不多并且运用上了，但是我总觉得自己有取巧的成分。
2. 用 `.style.cssText = " "`来动态添加样式，因为我在使用Ajax时，调用出来的文本不会采用头中的CSS，所以这里我选择了动态添加样式。
3. 同一个事件触发的函数写在一起，分开写会被**覆盖**。
### 考核进度
1. 主页算是完成，Ajax部分比较取巧，不知道师兄师姐是否认同我这个方法。就是把用户名的信息存在了一个txt文件中，利用Ajax调用出来，替换`div`中的文本。
2. 替换文本之后也花了很大功夫，给他加上样式，所以算是完成了吧（自我感觉，手动狗头）。
### 存在的问题
1. 如上，不知道我这方法是否可行。
---------------------
## 周记
* 生活随记
  * 确实，有很多的问题你明明看上去好像没啥问题，但是就是不行，会报错。我觉得这是不仅仅要在控制台里寻找，**在demo中不蹲尝试**，一点点摸索，虽然是挺慢的，但是明白之后就真的是恍然大悟，可以铭记吧，然后就是师兄说的确实挺对的，js dom编程艺术那本书现在确实是有点**过时**了，在网上查的方法要明显更简单一点，这也是与时俱进吧，要**充分利用**网上资源和书本。还有一个就是我发现自己**查百度的能力**还是不够，一个我查了很久的问题，其实一直都没找到点子上，都是自己想当然，结果询问了师兄之后，才恍然大悟，原来问题是出在这里，但是也一直都没想到，可以说找问题的能力还有查问题的能力都还需要加强。不过我觉得经过这一次，也算得到了锻炼。至少之后是可以掌握好方法吧。
* 学习进度
  
  * 主要就是每天记录的东西了。

* 学习难点

  * 同上

* 不足之处

  * 也确实会不专心，打着打着打烦了，就去看看番啥的了。
  * 安排时间也不太恰当，会有点与自己的作业有点冲突...
  * 这次都没有浪费太多时间在社团上了，但是自己总是要花很多时间在一些傻逼的问题上，也让我有点小烦躁。**润锋师兄说的**也挺对的，要是有一个地方想不明白，上课也会一直想，听不下去课...

* 下星期的学习安排

  * 所以还是希望我自己脑瓜子能聪明点吧，别在傻逼问题上花太多的时间....